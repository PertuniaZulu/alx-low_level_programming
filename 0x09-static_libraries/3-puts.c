#include "main.h"
void _puts(char *str);
#include <stdio.h>
/**
 *main- Entry point
 *_puts
 *Description: function that writes a line
 *or string to stdout
 *Return: Always 0 (success)
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
